Run category SOPs can now define custom fields to capture additional run data.

- Administrators can configure named fields with an optional unit and a value type.
- Supported field types are text, number, and percentage.
